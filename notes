* Ключевое auto слово — это простой способ объявления переменной со сложным типом. Например, можно использовать для auto 
объявления переменной, где выражение инициализации включает шаблоны, указатели на функции или указатели на члены.

* size_t = Предназначен для отображения размера любого объекта в байтах: это возвращает size_of и много функций стандартной библиотеки 

* Ключевое слово const позволяет подчеркнуть, что переменная используется только для чтения. (указатели)

* s2.size()

*  int* d[10] = {};

* #define SIZE 10

*cin.ignore() - ставим после всех cin и до первого getline() после тех cin
